# Arquimedes Docs

Use the folder that matches your role:

- `collaborator/` — read/search agent handbook for clients connecting to the maintainer's remote `arq-mcp` server.
- `maintainer/` — Mac maintainer operations, remote MCP publication, and recovery.
- `developer/` — implementation plan, pipeline internals, architecture notes, and phase specs under `developer/superpowers/`.
