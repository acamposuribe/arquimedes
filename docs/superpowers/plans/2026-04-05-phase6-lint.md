# Phase 6 — Wiki Linting, Reflection, and Memory Growth

> **Status:** Ready to implement
> **Spec:** [Phase 6 lint design](../specs/2026-04-05-phase6-lint-design.md)

## Goal

Implement `arq lint` as a two-stage maintenance system:
- deterministic health checks first
- reflective LLM passes second

The result should improve:
- cluster quality
- concept pages
- collection pages
- the memory layer

Implementation order:
1. deterministic lint core
2. cluster audit
3. concept reflection
4. collection reflection
5. memory feed
6. graph reflection

Parallelism rule:
- parallelize concept reflections across independent stale clusters
- parallelize collection reflections across independent stale collections
- keep graph reflection as a final global pass

## L6.1 Deterministic Lint Core

- [ ] Create `src/arquimedes/lint.py`
- [ ] Implement `run_deterministic_lint(config)` with checks for:
  - broken links
  - orphaned materials
  - orphaned wiki pages
  - missing metadata
  - stale enrichment
  - stale index
  - stale memory bridge
  - duplicates
  - missing compiled pages
- [ ] Write `derived/lint/deterministic_report.json`
- [ ] Add `arq lint --quick`
- [ ] Add deterministic-only tests

## L6.2 Cluster Audit Pass

- [ ] Add cluster-audit prompt + response schema
- [ ] Load current clusters and concept pages
- [ ] Emit `derived/lint/cluster_reviews.jsonl`
- [ ] Only audit stale clusters; prefer full audit for `material_count >= 2`
- [ ] Include findings for:
  - split recommendations
  - merge recommendations
  - weak single-material clusters
  - poor canonical names
  - missing materials in clusters
- [ ] Add tests for artifact shape and staleness behavior

## L6.3 Concept Reflection Pass

- [ ] Add concept-reflection prompt + response schema
- [ ] Input packet should include:
  - cluster identity
  - canonical name
  - aliases
  - supporting materials
  - current concept page
  - supporting evidence
- [ ] Emit `derived/lint/concept_reflections.jsonl`
- [ ] Only reflect stale clusters with `material_count >= 2` by default
- [ ] Include:
  - `main_takeaways`
  - `main_tensions`
  - `open_questions`
  - `why_this_concept_matters`
- [ ] Add tests

## L6.4 Collection Reflection Pass

- [ ] Add collection-reflection prompt + response schema
- [ ] Input packet should include:
  - deterministic collection page
  - member materials
  - linked concepts
  - key evidence
- [ ] Emit `derived/lint/collection_reflections.jsonl`
- [ ] Only reflect stale collections with at least 2 materials by default
- [ ] Include:
  - `main_takeaways`
  - `main_tensions`
  - `important_materials`
  - `open_questions`
- [ ] Add tests

## L6.5 Graph Reflection Pass

- [ ] Add graph-review prompt + response schema
- [ ] Emit `derived/lint/graph_findings.jsonl`
- [ ] Keep this pass advisory-first; do not require broad auto-application to pages
- [ ] Include:
  - missing cross-references
  - contradictions
  - under-connected materials
  - under-connected clusters
  - unanswered questions
  - candidate future sources/source-types
  - candidate bridge links/pages
- [ ] Add tests

## L6.6 Materialization Into Wiki

- [ ] Define deterministic page-update helpers for:
  - concept reflection sections
  - collection reflection sections
- [ ] Write human-readable `wiki/_lint_report.md`
- [ ] `arq lint --fix` should be able to apply accepted reflection outputs
- [ ] Keep reflective sections generated from `derived/lint/*`, not ad hoc markdown edits
- [ ] Add tests for page updates

## L6.7 Feed Memory

- [ ] Extend memory projection so reflective outputs become queryable
- [ ] Keep reflection tables/records distinct from cluster-topology tables
- [ ] Add structured reflection records for:
  - concept takeaways
  - concept tensions
  - concept open questions
  - collection takeaways
  - collection tensions
  - collection open questions
- [ ] Ensure `arq memory rebuild` runs after lint-applied changes
- [ ] Add tests proving reflective records are queryable from SQLite

## L6.8 CLI + Reporting

- [ ] Add:
  - `arq lint`
  - `arq lint --quick`
  - `arq lint --full`
  - `arq lint --report`
  - `arq lint --fix`
- [ ] Human-readable output
- [ ] JSON output
- [ ] Severity levels
- [ ] Exit codes for deterministic failures
- [ ] Review-friendly wording so findings can be filed and revisited later

## L6.9 Scheduling Model

- [ ] Document and implement:
  - `quick` after compile
  - `full` on periodic schedule
- [ ] Ensure expensive LLM passes can skip when reflection artifacts are current
- [ ] Add coarse schedule gates for the global graph pass

## Definition Of Done

- [ ] Deterministic lint works without LLM
- [ ] Full lint produces structured review artifacts
- [ ] Concept pages gain reflective sections
- [ ] Collection pages gain reflective sections
- [ ] Memory rebuild indexes those reflections
- [ ] Agents can query not only facts and links, but also takeaways and open questions
