# Arquimedes local configuration template
# Copy this file to config.local.yaml and adjust for your device.
# config.local.yaml is gitignored — it won't be committed.
# Only uncomment settings you need to override from config.yaml defaults.

# Path to the shared raw materials library.
# On macOS with iCloud, this is typically:
#   ~/Arquimedes-Library
# Adjust if your iCloud folder or OS is different.
library_root: "~/Arquimedes-Library"

# Legacy LLM agent CLI fallback.
# Used only when enrichment.llm_routes is not configured.
# The agent authenticates with its own credentials — no API keys needed here.
# llm:
#   agent_cmd:
#     - "claude --print"      # legacy fallback
#     - "codex exec"          # legacy fallback

# Extraction settings
# extraction:
#   chunk_size: 500            # tokens per chunk
#   generate_thumbnails: true
#   ocr_fallback: true
#   figure_extraction:
#     embedded: true
#     rasterize: true
#     rasterize_dpi: 200

# Enrichment settings
# enrichment:
#   prompt_version: "enrich-v1.0"
#   parallel: 4                # concurrent material enrichments (1 = sequential)
#   llm_routes:              # authoritative stage-specific routing
#     document:
#       - provider: codex
#         command: "codex exec"
#         model: gpt-5.4-mini
#         effort: high
#       - provider: copilot
#         command: "copilot"
#         model: gpt-5-mini
#         effort: high
#     chunk:
#       - provider: copilot
#         command: "copilot"
#         model: gpt-5-mini
#     figure:
#       - provider: codex
#         command: "codex exec"
#         model: gpt-5.4-mini
#         effort: medium
#       - provider: copilot
#         command: "copilot"
#         model: gpt-4o
#     cluster:
#       - provider: claude
#         command: "claude --print"
#         model: sonnet
#         effort: medium
#       - provider: codex
#         command: "codex exec"
#         model: gpt-5.4-mini
#         effort: high
#       - provider: copilot
#         command: "copilot"
#         model: gpt-5-mini
#
#   # Legacy single-agent settings only. Leave these commented if using
#   # llm_routes above.
#   effort:
#     document: medium
#     chunk: low
#     figure: low
#     cluster: medium
#   model:
#     document: sonnet
#     chunk: haiku
#     figure: haiku
#     cluster: sonnet
#   codex_model: gpt-5.4-mini
#   codex_effort:
#     document: high
#     chunk: medium
#     figure: medium
#     cluster: high

# Watch daemon settings (server mode)
# watch:
#   backend: fsevents          # fsevents (macOS) or poll (fallback)
#   poll_interval: 30
#   debounce_window: 10
#   batch_commit: true

# Sync daemon settings (collaborator mode)
# sync:
#   pull_interval: 300
#   auto_start: false
#   auto_index: true

# Web UI
# serve:
#   host: "0.0.0.0"
#   port: 8420
