# Arquimedes vault shared configuration (example).
# `arq init` writes the same shape automatically when you scaffold a new vault.

library_root: "~/Library/Mobile Documents/com~apple~CloudDocs/Arquimedes"

sync:
  pull_interval: 300
  auto_start: false
  auto_index: true
  reset_tracked: true

serve:
  host: "0.0.0.0"
  port: 8420
